# FluxForge API
## Overview
The FluxForge API is a high-performance API for managing complex data pipelines. It provides endpoints for creating, reading, updating, and deleting data pipelines.
## Installation
To install the dependencies, run `npm install`.
## Running the API
To start the API, run `node src/index.js`.